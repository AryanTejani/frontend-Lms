// Re-export feature stores for backwards compatibility and convenience
// Individual features own their stores, but we re-export here for easy access

// Global UI stores (create as needed)
// export { useUIStore } from './ui.store';
// export { useSidebarStore } from './sidebar.store';
// export { useModalStore } from './modal.store';
